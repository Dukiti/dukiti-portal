# Sao chép file này thành .env.local rồi điền URL Worker sau khi deploy.
# .env.local đã có trong .gitignore, không bị commit lên.
VITE_REPOS_PROXY_URL=https://dukiti-github-repos.<your-subdomain>.workers.dev
